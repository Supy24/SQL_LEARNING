-------Basic Filtering Questions

SELECT * FROM Employees

-------Select all employees with a salary greater than 50,000.

SELECT * 
FROM Employees
WHERE Salary > 50000

-------Find all employees hired before January 1, 2020.

SELECT *
FROM employees
WHERE hiredate < '2020-01-01';

-------List employees in the 'Marketing' department.

SELECT *
FROM Employees
WHERE Department = 'MARKETING'

-------Retrieve employees whose first names contain the letter 'a'.

SELECT *
FROM employees
WHERE FirstName LIKE '%A%'

-------Select employees with a salary of exactly 60,000.

SELECT *
FROM Employees
WHERE SALARY = 60000
